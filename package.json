{
  "name": "pte-app",
  "version": "0.0.0",
  "private": true,
  "description": "Production PTE Academic learning platform",
  "scripts": {
    "validate:docs": "node scripts/validate-docs.mjs",
    "test:docs": "node --test tests/docs/*.test.mjs",
    "test": "npm run test:docs && npm run validate:docs"
  },
  "engines": {
    "node": ">=24"
  }
}
